Beacons
=========

Installation
============

Install CocaoPods if you don't have it already
$ sudo gem install cocoapods -V
$ pod setup

Install all dependencies 
$ pod install

This will create a .xcworkspace file. Always open this file instead of the .xcodeproj file

